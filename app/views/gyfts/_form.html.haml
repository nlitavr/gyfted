= form_for @gyft do |f|
  - if @gyft.errors.any?
    #error_explanation
      %h2= "#{pluralize(@gyft.errors.count, "error")} prohibited this gyft from being saved:"
      %ul
        - @gyft.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_field :description
  .field
    = f.label :desire
    = f.text_field :desire
  .field
    = f.label :price
    = f.text_field :price
  .actions
    = f.submit 'Save'
